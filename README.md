# API 9 — Performance Variance Model

## Overview
This FastAPI application calculates the dimensional stability of a cricket all-rounder across batting, bowling, and fielding using the Coefficient of Variation (CV).

## Installation

1. Create a virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows use `venv\Scripts\activate`
